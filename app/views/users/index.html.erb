<% provide(:title, "All Users") %>

<h1>All Users!</h1>
<%= will_paginate %>
<ul class="users">
    <%= render @users %>
    <!-- the above code ititrates through and executes the partial based on CLASS USERS -->
</ul>
<%= will_paginate %>